{
	"Logging": {
		"LogLevel": {
			"Default": "Warning",
			"Microsoft": "Warning",
			"Microsoft.Hosting.Lifetime": "Warning"
		}
	},
	"AllowedHosts": "*",

	"ConnectionStrings": {
		"DBConnectionString": "Server=localhost;Database=JitbitHelpDesk;User Id=sa;Password=HDPassword1;Trusted_Connection=False;"
	},

	//"Auth": "Windows", //uncomment this line to enable windows-integrated authentication

	"SendEmailErrorReports": "false",
	"EmailErrorTo": "support@myserver.com", // separate multiple addresses with commas-->
	"EmailErrorFrom": "admin@myserver.com",
	"EmailErrorSMTPHost": "mail.myserver.com",
	"EmailErrorSMTPPort": "25",
	"EmailErrorUseSSL": "false",
	"EmailErrorSMTPUser": "admin@myserver.com",
	"EmailErrorSMTPPassword": "password", // just leave blank if your server does not use authentication

	//try to log errors to the server event log
	"LogErrors": true,

	//do not touch, advanced setting (leave blank to store files in the db)
	"AttachmentsPath": "",

	//do not touch, this disables the mail-importer, for debugging only
	"DisableMailChecker": "",

	//disallow users to change their avatars (if you want them to come from AD for example)
	"DisallowChangingAvatars": "false",

	// "AppURL" :"http://support.yourcompany.com", //this URL will be used in all outgoing email notifications

	//If you're using Windows authentication - this user will be added to administrators automatically
	"WindowsAdminUsername": "",

	/* (do not touch. andvanced setting)
    email-server passwords will be encrypted in the db with this key (if not empty). 8 characters, latin letters/numbers only. example "78d6fg81"
    BACKUP THIS KEY AND NEVER CHANGE IT otherwise your email password in the DB will be lost forever!! */
	"MailPswEncryptKey": "",

	// disables SSL 3 and TLS 1 and 1.1 for all network connections-->
	"DisableWeakCiphers": "false"
}
